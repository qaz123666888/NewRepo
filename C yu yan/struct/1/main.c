#include <stdio.h>
void change(int n){
    if(n>1){
        change(n-1);
    }
    printf
}
int main() {
     struct student s;
    scanf("%d%s %c",&s.num,s.name,&s.sex);
    printf("%d %s %c",s.num,s.name,s.sex);
    return 0;
}
